<Page x:Class="Rent_a_Car.MVVM.View.CarsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rent_a_Car" 
             xmlns:viewModel="clr-namespace:Rent_a_Car.MVVM.ViewModel"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450"
             d:DesignWidth="700"
            >
    <Page.DataContext>
        <viewModel:CarsViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <Style x:Key="ElevatedCardStyle" TargetType="materialDesign:Card">
            <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp24" />
                    <Setter Property="BorderBrush"  Value="White" />
                    <Setter Property="BorderThickness"  Value="2" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="{x:Type TextBox}" x:Key="ModernTextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border CornerRadius="10"
                        Background="#676678"
                        Width="200"
                        Height="40">
                            <Grid>
                                <Rectangle StrokeThickness="1"/>
                                <TextBox Margin="1" 
                                 Text="{TemplateBinding Text}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalContentAlignment="Center"
                                 Padding="5"
                                         
                                 Foreground="#CFCFCF"
                                 x:Name="SearchBox"/>
                                <TextBlock IsHitTestVisible="False"
                                   Text="Search"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Left"
                                   Margin="10,0,0,0"
                                   FontSize="11"
                                   Grid.Column="1"
                                   >
                                    <TextBlock.Style>
                                        <Style TargetType="{x:Type TextBlock}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text,ElementName=SearchBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="Visibility" Value="Hidden"/>
                                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                             
                                        </Style>
                                    </TextBlock.Style>

                                </TextBlock>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ScrollViewer.Template>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <ScrollContentPresenter />
                        <ScrollBar x:Name="PART_VerticalScrollBar" Visibility="Collapsed" />
                        <ScrollBar x:Name="PART_HorizontalScrollBar" Visibility="Collapsed" />
                    </Grid>
                </ControlTemplate>
            </ScrollViewer.Template>
            <StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Discover"
                           Foreground="White"
                           FontSize="28"
                           HorizontalAlignment="Left" 
                           Margin="20,0,240,20"/>
                    <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- TextBox za unos -->
                    <TextBox x:Name="SearchTextBox" 
                                Grid.Row="0" Grid.Column="0"
                                Margin="0,0,10,10"
                                Height="40" Width="250"
                                Foreground="White"
                                VerticalAlignment="Top"
                                VerticalContentAlignment="Center"
                                BorderThickness="0"
                                Style="{StaticResource ModernTextBox}"/>
                    </Grid>
                </StackPanel>
                
                <TextBlock Text="Most Watched"
                           Foreground="White"
                           FontSize="28"
                           HorizontalAlignment="Left" 
                           Margin="20,10,0,10"/>
                <UniformGrid Columns="3" Rows="3" Margin="0,10,0,40">

                   
                    <materialDesign:Card Width="190"
                     Background="{DynamicResource CarInfoBorderColor}"
                     Foreground="White" 
                     
                     Style="{StaticResource ElevatedCardStyle}">
                        <materialDesign:Card.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding CarInfoViewCommand}" />
                        </materialDesign:Card.InputBindings>
                        <Grid Margin="-1,0,1,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="140" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Image Width="196"
                                    Height="140"
                                    Source="C:\Users\User\OneDrive\Radna površina\Projekat A\cars\renault4\thumb.jpg"
                                    Stretch="UniformToFill" Margin="-5,-2,-1,2" />
                           
                            <StackPanel Grid.Row="1" Margin="8,24,8,0">
                                <TextBlock FontWeight="Bold">Cycling</TextBlock>
                                <TextBlock VerticalAlignment="Center"
                                           Text="A great way to keep fit and forget about the constant grind of IT."
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Margin="8" HorizontalAlignment="Right" Orientation="Horizontal">
                                <Button Width="30"
                                        Padding="2,0,2,0"
                                        materialDesign:RippleAssist.IsCentered="True"
                                        Style="{StaticResource MaterialDesignToolButton}">
                                    <materialDesign:PackIcon Foreground="White" Kind="ShareVariant" />
                                </Button>
                                <Button Width="30"
                                        Padding="2,0,2,0"
                                        materialDesign:RippleAssist.IsCentered="True"
                                        Style="{StaticResource MaterialDesignToolButton}">
                                    <materialDesign:PackIcon Foreground="White" Kind="Heart" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Width="190"
                                    Background="{DynamicResource CarInfoBorderColor}"
                                    Foreground="White" 
 
                        Style="{StaticResource ElevatedCardStyle}">
                        <materialDesign:Card.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding CarInfoViewCommand}" />
                        </materialDesign:Card.InputBindings>
                        <Grid Margin="-1,0,1,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="140" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Image Width="196"
                Height="140"
                Source="C:\Users\User\OneDrive\Radna površina\Projekat A\cars\renault4\thumb.jpg"
                Stretch="UniformToFill" Margin="-5,-2,-1,2" />

                            <StackPanel Grid.Row="1" Margin="8,24,8,0">
                                <TextBlock FontWeight="Bold">Cycling</TextBlock>
                                <TextBlock VerticalAlignment="Center"
                       Text="A great way to keep fit and forget about the constant grind of IT."
                       TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Margin="8" HorizontalAlignment="Right" Orientation="Horizontal">
                                <Button Width="30"
                    Padding="2,0,2,0"
                    materialDesign:RippleAssist.IsCentered="True"
                    Style="{StaticResource MaterialDesignToolButton}">
                                    <materialDesign:PackIcon Foreground="White" Kind="ShareVariant" />
                                </Button>
                                <Button Width="30"
                    Padding="2,0,2,0"
                    materialDesign:RippleAssist.IsCentered="True"
                    Style="{StaticResource MaterialDesignToolButton}">
                                    <materialDesign:PackIcon Foreground="White" Kind="Heart" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Width="190"
 Background="{DynamicResource CarInfoBorderColor}"
 Foreground="White" 
 
 Style="{StaticResource ElevatedCardStyle}">
                        <materialDesign:Card.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding CarInfoViewCommand}" />
                        </materialDesign:Card.InputBindings>
                        <Grid Margin="-1,0,1,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="140" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Image Width="196"
                Height="140"
                Source="C:\Users\User\OneDrive\Radna površina\Projekat A\cars\renault4\thumb.jpg"
                Stretch="UniformToFill" Margin="-5,-2,-1,2" />

                            <StackPanel Grid.Row="1" Margin="8,24,8,0">
                                <TextBlock FontWeight="Bold">Cycling</TextBlock>
                                <TextBlock VerticalAlignment="Center"
                       Text="A great way to keep fit and forget about the constant grind of IT."
                       TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Margin="8" HorizontalAlignment="Right" Orientation="Horizontal">
                                <Button Width="30"
                    Padding="2,0,2,0"
                    materialDesign:RippleAssist.IsCentered="True"
                    Style="{StaticResource MaterialDesignToolButton}">
                                    <materialDesign:PackIcon Foreground="White" Kind="ShareVariant" />
                                </Button>
                                <Button Width="30"
                    Padding="2,0,2,0"
                    materialDesign:RippleAssist.IsCentered="True"
                    Style="{StaticResource MaterialDesignToolButton}">
                                    <materialDesign:PackIcon Foreground="White" Kind="Heart" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>

                    <materialDesign:Card Width="190" Margin="0,15,0,0"
                                        Background="{DynamicResource CarInfoBorderColor}"
                                        Foreground="White" 
                                        Style="{StaticResource ElevatedCardStyle}">
                        <materialDesign:Card.InputBindings>
                            <MouseBinding MouseAction="LeftClick" Command="{Binding CarInfoViewCommand}" />
                        </materialDesign:Card.InputBindings>
                        <Grid Margin="-1,0,1,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="140" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Image Width="196"
                Height="140"
                Source="C:\Users\User\OneDrive\Radna površina\Projekat A\cars\renault4\thumb.jpg"
                Stretch="UniformToFill" Margin="-5,-2,-1,2" />

                            <StackPanel Grid.Row="1" Margin="8,24,8,0">
                                <TextBlock FontWeight="Bold">Cycling</TextBlock>
                                <TextBlock VerticalAlignment="Center"
                       Text="A great way to keep fit and forget about the constant grind of IT."
                       TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Grid.Row="2" Margin="8" HorizontalAlignment="Right" Orientation="Horizontal">
                                <Button Width="30"
                    Padding="2,0,2,0"
                    materialDesign:RippleAssist.IsCentered="True"
                    Style="{StaticResource MaterialDesignToolButton}">
                                    <materialDesign:PackIcon Foreground="White" Kind="ShareVariant" />
                                </Button>
                                <Button Width="30"
                    Padding="2,0,2,0"
                    materialDesign:RippleAssist.IsCentered="True"
                    Style="{StaticResource MaterialDesignToolButton}">
                                    <materialDesign:PackIcon Foreground="White" Kind="Heart" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </materialDesign:Card>


                </UniformGrid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
