<UserControl x:Class="CarRentalManagment.Views.VehiclesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:CarRentalManagment.Views"
             mc:Ignorable="d"
             d:DesignHeight="900"
             d:DesignWidth="1600"
             Background="#FF1A1D2E">

    <!-- Main container showing just the vehicle list -->
    <views:VehicleListView DataContext="{Binding VehicleList}" />

    <!-- Alternative: If you want to show both list and details side-by-side, use this instead:
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        
        <views:VehicleListView Grid.Column="0" DataContext="{Binding VehicleList}" />
        <views:VehicleDetailsView Grid.Column="1" DataContext="{Binding VehicleDetails}" />
    </Grid>
    -->
</UserControl>