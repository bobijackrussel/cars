<UserControl x:Class="CarRentalManagment.Views.SectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Background="{DynamicResource PageBackgroundBrush}">
        <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="20" Padding="32" Margin="32">
            <StackPanel>
                <TextBlock Text="{Binding Title}"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock Text="{Binding Description}"
                           FontSize="14"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           TextWrapping="Wrap"
                           Margin="0,12,0,0" />
                <Border Background="{DynamicResource CardAltBackgroundBrush}"
                        CornerRadius="12"
                        Padding="20"
                        Margin="0,20,0,0">
                    <TextBlock Text="This section is ready for future implementation."
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               FontSize="13" />
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
